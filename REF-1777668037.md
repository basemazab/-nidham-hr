# REF-1777668037 — HR-BASEM-AZAB-HRMS MVP (نظام, Nidham)

---

## Action Required (Critical)

Root cause of persistent errors (ref `18170709` and `615811798`) on automation page:  
**Database migrations 071–075 have never been run against Supabase.**  

The following tables do not exist in the database, breaking every feature from Phase 3 onward:
- `workflows` (Phase 3 — Automation)
- `api_keys`, `api_usage_logs` (Phase 5 — API Infrastructure)
- `ai_conversations`, `ai_messages`, `ai_knowledge_base`, `ai_audit_log` (Phase 6 — AI Memory & RAG)
- `notifications` (Phase 8 — Mobile UX)
- `company_roles`, `company_role_permissions`, `permission_defs`, `user_sessions`, `security_events` (Phase 9 — Security & Compliance)

### Fix
Run the combined migration file in Supabase SQL Editor:
- **`nidham/db/migrations/071-075_combined.sql`** (all 5 migrations in one file)

> **⚠️ Prerequisite**: Enable the `vector` extension in Supabase Dashboard → Database → Extensions before running. Without it, the `CREATE EXTENSION IF NOT EXISTS vector` statement in migration 074 will fail.

---

## Project Identity

| Field | Value |
|-------|-------|
| **Ref** | REF-1777668037 |
| **Path** | `E:\sys\HR-BASEM-AZAB-HRMS-devin-1777668037-hrms-mvp` |
| **System name** | نظام (Nidham) |
| **Type** | Arabic-first HRMS (HR Management System) |
| **Stack** | Next.js 14 (App Router), Supabase (PostgreSQL + Auth + Storage), Tailwind CSS |
| **Primary language** | TypeScript (React Server Components) |
| **Interface language** | Arabic (RTL) |

---

## Phases & Status

| # | Phase | Status | Details |
|---|-------|--------|---------|
| 1 | Foundation & Auth | ✅ Complete | Next.js 14 setup, Supabase auth, RLS policies 001–070 run |
| 2 | Core HR Modules | ✅ Complete | Employees, attendance, leaves, payroll, advances, touristic teams |
| 3 | Workflow Automation | ❌ Broken | Tables missing — `071_workflows.sql` not run |
| 4 | Self-Service Portal | ✅ Complete | Employee dashboard, request forms |
| 5 | API Infrastructure | ❌ Broken | Tables missing — `072_api_keys.sql` not run |
| 6 | AI Memory & RAG | ❌ Broken | Tables missing — `074_ai_memory_rag.sql` not run |
| 7 | Executive Analytics | ✅ Complete | Existing analytics page with charts, KPIs, payroll insights |
| 8 | Mobile UX Enhancement | 🟡 Partial | PWA service worker created (not active in layout), notification components created (not imported), clock-in page restored from git |
| 9 | Security & Compliance | ❌ Broken | Tables missing — `073_granular_permissions.sql` not run |

---

## Tests

| Suite | Count | Status |
|-------|-------|--------|
| E2E (Playwright) | 18 tests, 6 suites | ✅ Passing |
| Unit (Vitest) | 53 tests | ✅ Passing |

Run tests with:
```
cd nidham
npx vitest run          # unit
npx playwright test     # e2e
```

---

## Tech Debt / Known Issues

1. **PWA service worker** (`public/sw.js`, `public/notification-icon.svg`, `app/api/push/route.ts`, `app/api/vapid-config/route.ts`) — created but **not imported** in `app/layout.tsx`. PWA is inactive.
2. **Notification components** (`NotificationBell`, `PushNotificationHandler`) — created but **not imported** anywhere.
3. **PWA InstallPrompt** component — created but **not imported**.
4. **Icon SVG** (`public/icon.svg`) — created; `public/manifest.json` already exists.
5. **Executive analytics** — connection to Payroll data via `getPayrollAnalytics` already wired; no new files needed.
6. **All phases 3, 5, 6, 9** — depend on migrations 071–075. Until those are run, the automation page and several other features will throw 400 errors.

---

## Notes

- `.env.local` uses NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY (values NOT committed)
- Migrations 001–070 are already applied to Supabase
- Running migrations 071–075 will unblock ALL broken features
- No additional code changes needed after running migrations — the application code already references the missing tables
